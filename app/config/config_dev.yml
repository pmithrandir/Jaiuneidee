imports:
    - { resource: config.yml }

framework:
    router:   { resource: "%kernel.root_dir%/config/routing_dev.yml" }
    profiler: { only_exceptions: false }
web_profiler:
    toolbar: true
    intercept_redirects: false

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: debug
        firephp:
            type:  firephp
            level: info

#assetic:
#    use_controller: true
fos_user:
    registration:
      # Permet la validation automatique du compte par envoi d'un email
      confirmation:
        enabled:    true

swiftmailer:
    delivery_address:  pmithrandir@gmail.com
    #disable_delivery:  true